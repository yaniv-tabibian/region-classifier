name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"
          # Check the source tree under src/ (mypy_path / pytest pythonpath), not the
          # editable install — its PEP 660 finder breaks mypy's package discovery on
          # some runners (was failing only on 3.12).
          pip uninstall -y region-classifier
      - name: Lint (ruff)
        run: ruff check .
      - name: Format check (black)
        run: black --check src tests
      - name: Type check (mypy)
        run: mypy
      - name: Tests
        run: pytest -q
